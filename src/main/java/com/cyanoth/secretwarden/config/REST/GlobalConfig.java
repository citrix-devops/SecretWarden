package com.cyanoth.secretwarden.config.REST;

import javax.ws.rs.PUT;
import javax.ws.rs.Path;
import javax.ws.rs.core.Response;

public class GlobalConfig  {
//    private static final String PLUGIN_KEY = "com.cyanoth.secretwarden"; // This _must_ to remain hardcoded incase of changes to the plugin name.
//    private static final Logger log = LoggerFactory.getLogger(GlobalConfig.class);
//
//    private final PluginSettingsFactory pluginSettingsFactory;
//    private final SecurityService securityService;
//    private final PermissionValidationService permissionValidationService;
//
//    public GlobalConfig(PluginSettingsFactory pluginSettingsFactory, SecurityService securityService,
//                        PermissionValidationService validationService) {
//
//        pluginSettingsFactory
//
//        pluginSettings = pluginSettingsFactory.createSettingsForKey(PLUGIN_KEY);
//    }


        //@PUT
    //@Path("/clearcache")
    //  public Response clearCache()
    //{
//        UserKey userKey = userManager.getRemoteUserKey(request);
//        if (userKey == null || !userManager.isAdmin(userKey))
//        {
//            redirectToLogin(request, response);
//            return;
//        }

      //  pullRequestSecretScanResultCache.clear();
     //   return Response.ok("Cache Cleared!").build();
    //}

}
